/* bouts de code poubelles
/*    while (1) 
	{
	SDL_PollEvent(e->eve);
        if (e->eve->type == SDL_QUIT)
            break;
//		SDL_FillRect(e->wsu, NULL, SDL_MapRGB(e->wsu->format, 0xAA, 0xBB, 0xCC));
//		SDL_SetRenderDrawColor(e->ren, 10, 10, 10, 255);
//		SDL_RenderDrawPoint(e->ren, 50, 50);
//		SDL_UpdateWindowSurface(e->win);
//		SDL_SetRenderDrawColor(e->ren, 0xAA, 0xBB, 0xCC, 0xFF);
//     	SDL_RenderClear(e->ren);
//    	SDL_RenderCopy(renderer, texture, NULL, NULL);
//  	SDL_RenderPresent(e->ren);
    } */
/*

	SDL_SetRenderDrawColor(e->ren, 0xAA, 0xBB, 0xCC, 255); // choix de la couleur de l'outil
  	SDL_RenderClear(e->ren);	// remplissage du render
  	SDL_RenderPresent(e->ren);	// affichage du render
	SDL_Delay(2000);
	test(e->ren);
	SDL_Delay(2000);
	test(e->ren);
	SDL_Delay(2000);
	test(e->ren);
	SDL_Delay(5000);

	*/